לרשום גיט לפני כל מילה
cd .. לחזור אחורה
ls להסתכל על התקייה הנוכחית
pwd לראות את הנתיב
cd והשם של התקייה

למשוך קבצים:
1. להגיע לתיקיית FamilyBox
2. לכתוב git pull origin master
3. אם עושה בעיות, לכתוב git pull

להכניס קובץ שעבדת עליו:
להגיע לתיקייה FamilyBox
לכתוב: git add fileName.html
לכתוב: git commit -m "notification"
לכתוב: git push git
git add -a
git add .


לבדוק שינויים 
git log
git status

branches

git log --online בודק לוגים
git branch (name of feature) - (יוצר brach חדש)
git branch -a (מראה את כל נתיבי הbarnches)
git checkout (name of feature) (עובר לbrach)
git branch -D (name of feature) (מוחק את הbrach מהmaster)
git branch -D (name of feature) (מוחק את הbrach מהbrach)
